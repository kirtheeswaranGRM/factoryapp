\"const { Pool } = require('pg'); const pool = new Pool({ user: 'postgres', host: 'localhost', database: 'postgres', password: 'Post@123', port: 5432 }); pool.query(\\\"SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'\\\", (err, res) => { if (err) { console.error(err); } else { console.log(JSON.stringify(res.rows.map(r => r.table_name))); } pool.end(); });\"  
